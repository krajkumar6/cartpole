1.Install Anaconda for Mac OS through the installer
2.create a conda environment with a Python version 3 using the anaconda-navigator
3.Install gym for class control environments using below command
    pip install gym[classic_control]
4.Install pytorch using below command
    conda install pytorch torchvision torchaudio -c pytorch
5.Install matplotlib using below command
    conda install -c conda-forge matplotlib